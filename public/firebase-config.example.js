/* =============================================================================
 * firebase-config.example.js  (FASE 3 — online)
 * -----------------------------------------------------------------------------
 * Kopieer naar firebase-config.js en vul je eigen Firebase-projectgegevens in.
 * firebase-config.js staat in .gitignore (geheimen niet in GitHub).
 *
 * Zolang dit niet is ingevuld, draait de app in OFFLINE modus (localStorage).
 * ========================================================================== */
window.FIREBASE_CONFIG = {
  apiKey: "VUL_IN",
  authDomain: "VUL_IN.firebaseapp.com",
  projectId: "VUL_IN",
  storageBucket: "VUL_IN.appspot.com",
  messagingSenderId: "VUL_IN",
  appId: "VUL_IN"
};
